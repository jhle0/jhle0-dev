---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime())
  .slice(0, 3);

const projects = (await getCollection("projects"))
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime())
  .slice(0, 3);
---

<BaseLayout
  title="Home | jhle0"
  description="Study notes, projects, and logs in one place."
>
  <section class="hero">
    <h1>jhle0</h1>
    <p>Study notes, projects, and logs in one place.</p>
    <p>I write about computer science, AI, projects, and what I am learning.</p>
  </section>

  <section>
    <h2>Recent Posts</h2>
    {
      posts.length === 0 ? (
        <p>No posts yet.</p>
      ) : (
        <ul class="card-list">
          {posts.map((post) => (
            <li>
              <article>
                <h3>
                  <a href={`/blog/${post.data.slug}`}>{post.data.title}</a>
                </h3>
                <p>{post.data.description}</p>
              </article>
            </li>
          ))}
        </ul>
      )
    }
    <p><a href="/blog">See all posts →</a></p>
  </section>

  <section>
    <h2>Recent Projects</h2>
    {
      projects.length === 0 ? (
        <p>No projects yet.</p>
      ) : (
        <ul class="card-list">
          {projects.map((project) => (
            <li>
              <article>
                <h3>
                  <a href={`/projects/${project.data.slug}`}>
                    {project.data.title}
                  </a>
                </h3>
                <p>{project.data.description}</p>
              </article>
            </li>
          ))}
        </ul>
      )
    }
    <p><a href="/projects">See all projects →</a></p>
  </section>

  <section>
    <h2>Explore</h2>
    <ul class="inline-list">
      <li><a href="/about">About</a></li>
      <li><a href="/now">Now</a></li>
      <li><a href="/contact">Contact</a></li>
    </ul>
  </section>
</BaseLayout>
